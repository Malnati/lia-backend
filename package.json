{
  "name": "lia-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "wrangler": "wrangler",
    "test:e2e": "vitest run test/e2e"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.2",
    "hono": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "latest",
    "vitest": "latest",
    "wrangler": "latest"
  },
  "description": "Lia backend — API Cloudflare Workers + Hono real para Supabase/Postgres, pedidos, checkpoints, anexos e pagamentos.",
  "license": "MIT",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=22.14.0",
    "pnpm": ">=10.0.0"
  }
}
